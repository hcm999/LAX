{
  "name": "usdt-staking-dapp",
  "version": "1.0.0",
  "description": "USDT Staking DApp",
  "scripts": {
    "start": "serve dist",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r assets dist/",
    "dev": "serve"
  },
  "dependencies": {
    "ethers": "^6.7.0",
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "@vercel/static-build": "^1.0.0"
  }
}
